#include your zendesk email, subdomain (not full domain), and password
zendesk_password = "this is your password"
email = 'you@some_subdomain.com'
subdomain = 'some_subdomain'
#the id that all old legacy events will be imported under. You need to create this
legacy_id = raw_input("What is the legacy user id you'd like to use?")